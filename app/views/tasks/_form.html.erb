<%= form_for([project, task], html: {class: 'ui form'}) do |f| %>
  <% if task.errors.any? %>
    <div id='error_explanation'>
      <h2><%= pluralize(task.errors.count, 'error') %> prohibited this task from being saved:</h2>

      <ul>
      <% task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class='field'>
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class='field'>
    <%= f.label :body %>
    <%= f.text_area :body %>
  </div>

  <div>
    <%= f.submit class: 'ui button primary'%>
  </div>
<% end %>
